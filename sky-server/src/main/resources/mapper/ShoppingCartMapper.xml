<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.ShoppingCartMapper">
	<!-- 通过用户id查询当前所有的购物车信息
		再通过菜品id或套餐id查询当前购物车菜品或套餐内容
	 -->
	<select id="selectShoppingCartByUserId" resultType="com.sky.entity.ShoppingCart">
		select * from shopping_cart
		<where>
			<if test="dishId != null">
				and dish_id = #{dishId}
			</if>
			<if test="setmealId != null">
				and setmeal_id = #{setmealId}
			</if>
			<if test="dishFlavor != null and dishFlavor != ''">
				and dish_flavor = #{dishFlavor}
			</if>
				and user_id = #{userId}
		</where>
	</select>
	
	<update id="update">
		update shopping_cart
		<set>
			<if test="number != null">
				number = #{number},
			</if>
		</set>
		where id = #{id}
	</update>
	
	<insert id="insert">
		insert into shopping_cart (name,user_id,dish_id,setmeal_id,dish_flavor,number,amount,image,create_time) 
		values(#{name},#{userId},#{dishId},#{setmealId},#{dishFlavor},#{number},#{amount},#{image},#{createTime})
	</insert>
</mapper>