<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">
	<!-- 添加菜品，通过数据回显展示主键 -->
	<insert id="addDish" useGeneratedKeys="true" keyProperty="id">
		insert into dish(name,category_id,price,image,description,status,create_time,update_time,create_user,update_user) values 
		(#{name},#{categoryId},#{price},#{image},#{description},#{status},#{createTime},#{updateTime},#{createUser},#{updateUser})
	</insert>
	<!-- 分页查询 -->
	<select id="pageQuery" resultType="com.sky.vo.DishVO">
		select * from dish d left join category c on d.category_Id = c.id
		<where>
			<if test="name != null and name != ''">
				and d.name like concat('%',#{name},'%')
			</if>
			<if test="categoryId != null">
				and d.category_id = #{categoryId}
			</if>
			<if test="status != null">
				and d.status = #{status}
			</if>
		</where>
		order by d.create_time desc
	</select>
	<!-- 查询 新增套餐时添加菜品用 -->
	<select id="list" resultType="com.sky.entity.Dish">
	    select * from dish
	    <where>
	        <if test="categoryId != null">
	            and category_id = #{categoryId}
	        </if>
	        <if test="status != null">
	            and status = #{status}
	        </if>
	    </where>
	    order by create_time desc
	</select>
	<!-- 修改菜品 -->
	<update id="updateDish" parameterType="com.sky.entity.Dish">
		update dish
		<set>
			<if test="name != null">
				name = #{name},
			</if>
			<if test="categoryId != null">
				category_id = #{categoryId},
			</if>
			<if test="price != null">
				price = #{price},
			</if>
			<if test="image != null">
				image = #{image},
			</if>
			<if test="description != null">
				description = #{description},
			</if>
			<if test="status != null">
				status = #{status},
			</if>
			<if test="updateTime != null">
				update_time = #{updateTime},
			</if>
			<if test="updateUser != null">
				update_user = #{updateUser}
			</if>
		</set>
		where id = #{id}
	</update>
	<!--修改菜品状态-->
	<update id="updateStatus">
	    update dish set status=#{status},update_time=now() where id=#{id}
	</update>
	<!--  统计停售菜品数量 -->
	<select id="countStopDishByIds" resultType="java.lang.Integer">
	    select count(*) from dish
	    where status = 0
	    <foreach collection="dishIds" item="id" open="and id in (" separator="," close=")">
	        #{id}
	    </foreach>
	</select>
	<select id="listByCategoryId" resultType="com.sky.entity.Dish">
	    select * from dish
	    where category_id = #{categoryId} and status = 1
	</select>
	
	
</mapper>